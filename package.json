{
  "name": "symbaroum-hud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate:release": "node --test tests/*.test.mjs"
  }
}
